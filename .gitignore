# All runtime data + outputs (PDB cache, manifests, tensors, examples,
# epitope-pipeline scratch, design/refold outputs, smoke artifacts, wandb,
# logs, dunbrack vendored dataset, per-run PDB lists)
twistr/runtime/data/pdb/
twistr/runtime/data/manifests/
twistr/runtime/data/aux/
twistr/runtime/data/dunbrack/
twistr/runtime/data/tensors/
twistr/runtime/data/examples/
twistr/runtime/data/epitopes/
twistr/runtime/data/linkers/
twistr/runtime/data/agent_inputs/
twistr/runtime/data/checkpoints/
twistr/runtime/data/targets*.txt
twistr/runtime/outputs/

# Local caches (Protenix weights, PXDesign MSAs, batch calibration, etc.)
.cache/

# Run logs and PIDs (anywhere)
*.log
*.pid

# ML training artifacts (anywhere)
lightning_logs/
mlruns/
checkpoints/
*.ckpt
*.pt
*.pth

# Stray large binaries — defense-in-depth (anywhere)
*.npz
*.parquet
*.h5
*.hdf5
*.pkl
*.npy

# RunPod smoke-test scratch (generated locally, never committed)
twistr/dev/tools/runpod/smoke_test/.deploy_key
twistr/dev/tools/runpod/smoke_test/.deploy_key.pub
twistr/dev/tools/runpod/smoke_test/subset/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
.venv-rosetta/
venv/
dist/
build/
.ipynb_checkpoints/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Secrets / local env
.env
.envrc

# Local Claude Code project instructions
/CLAUDE.md
